server:
  port: 7001

eureka:
  instance:
    hostname: eureka7001.com
  client:
    register-with-eureka: false # 不向注册中心注册自己，单机版
    fetch-registry: false # 自己就是注册中心，职责就是维护服务器实例，并不需要检索服务
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/